[package]
name = "nvidia-container-toolkit"
version = "0.1.0"
edition = "2021"
publish = false
build = "../build.rs"

[lib]
path = "../packages.rs"

[package.metadata.build-package]
releases-url = "https://github.com/NVIDIA/nvidia-container-toolkit/releases"

[[package.metadata.build-package.external-files]]
url = "https://github.com/NVIDIA/nvidia-container-toolkit/archive/v1.16.0/nvidia-container-toolkit-1.16.0.tar.gz"
sha512 = "7014614bdb5c4f22364b2d1baa8f49033079a5d67ddb94db12ea04d1b9258f760c7369c9a26e2374f34fa7f069d16513c1e38e6a27ed8dbe43a5f04d3395536e"

[build-dependencies]
glibc = { path = "../glibc" }
libnvidia-container = { path = "../libnvidia-container" }
# This package depends on `shimpei`, but it is built in the `os` package
# which is expected to be pulled in
# os = { path = "../os" }
